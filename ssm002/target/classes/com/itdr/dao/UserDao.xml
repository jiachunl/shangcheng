<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itdr.dao.UserDao">
    <resultMap id="BaseResultMap" type="com.itdr.pojo.User">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="userpic" jdbcType="VARCHAR" property="userpic" />
        <result column="email" jdbcType="VARCHAR" property="email" />
        <result column="phone" jdbcType="VARCHAR" property="phone" />
        <result column="question" jdbcType="VARCHAR" property="question" />
        <result column="answer" jdbcType="VARCHAR" property="answer" />
        <result column="role" jdbcType="INTEGER" property="role" />
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
        <result column="ip" jdbcType="VARCHAR" property="ip" />
        <result column="token" jdbcType="VARCHAR" property="token" />
    </resultMap>
    <select id="getAll" resultType="User">
        select * from neuedu_user
    </select>
    <select id="getUserByUnameAndPwd"  resultMap="BaseResultMap">
        select * from neuedu_user where username=#{username} and password=#{password}
    </select>
    <select id="getUsers" resultType="User">
        select * from neuedu_user
    </select>
    <insert id="addUsers">
        insert into neuedu_user(username,password,email,phone,question,answer,role,create_time,update_time)
        values
        (#{username},#{password},#{email},#{phone},#{question},#{answer},#{role},now(),now())
    </insert>
    <update id="updateUser" parameterType="com.itdr.dao.UserDao">
        update neuedu_user set username=#{username},password=#{password},phone=#{phone},email=#{email},question=#{question},answer=#{answer},role=#{role} where id=#{id}
    </update>
    <delete id="delUser" parameterType="com.itdr.dao.UserDao">
        delete from neuedu_user where id=#{id}
    </delete>
    <select id="update" resultType="User">
        select * from neuedu_user
    </select>
    <select id="getUserById" resultType="User">
        select * from neuedu_user where id=#{id}
    </select>
    <select id="getAllUser" resultType="User">
        select * from neuedu_user where id=#{id}
    </select>
    <select id="getUser" resultType="User">
        select * from neuedu_user where username=#{username}
    </select>
    <select id="isUserExist" resultType="Boolean">
        select * from neuedu_user where username=#{username}
    </select>
    <select id="getPasswordByUsername" resultType="String">
        select password from neuedu_user where username=#{username}
    </select>

    <update id="updatePwdByUname">
        update neuedu_user set password=#{password} where username=#{username}
    </update>
</mapper>
