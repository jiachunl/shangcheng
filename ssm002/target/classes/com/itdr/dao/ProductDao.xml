<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itdr.dao.ProductDao">
    <resultMap id="BaseResultMap" type="com.itdr.pojo.Product">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="category_id" jdbcType="INTEGER" property="categoryId" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="subtitle" jdbcType="VARCHAR" property="subtitle" />
        <result column="main_image" jdbcType="VARCHAR" property="mainImage" />
        <result column="price" jdbcType="DECIMAL" property="price" />
        <result column="stock" jdbcType="INTEGER" property="stock" />
        <result column="status" jdbcType="INTEGER" property="status" />
        <result column="is_new" jdbcType="BIT" property="isNew" />
        <result column="is_hot" jdbcType="BIT" property="isHot" />
        <result column="is_banner" jdbcType="BIT" property="isBanner" />
        <result column="sub_images" jdbcType="LONGVARCHAR" property="subImages" />
        <result column="detail" jdbcType="LONGVARCHAR" property="detail" />
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
    </resultMap>
    <select id="getPros" resultType="Product" resultMap="BaseResultMap">
        select * from neuedu_product
    </select>

    <insert id="addPro">
        insert into neuedu_product(name,main_image,detail,category_id,price,stock,status,create_time,update_time)
        values
        (#{name},#{mainImage},#{detail},#{categoryId},#{price},#{stock},#{status},now(),now())
    </insert>

    <update id="updateProduct" parameterType="com.itdr.dao.ProductDao">
        update neuedu_product set name=#{name},detail=#{detail},category_id=#{categoryId},price=#{price},stock=#{stock},status=#{status} where id=#{id}
    </update>

    <delete id="deleteProduct" parameterType="com.itdr.dao.ProductDao">
        delete from neuedu_product where id=#{id}
    </delete>

    <select id="getProductById" resultType="Product">
        select * from neuedu_product where id=#{id}
    </select>
    <select id="getAllProduct" resultType="Product">
        select * from neuedu_product where id=#{id}
    </select>
    <!--<select id="getProductAll" resultType="Product">
        select * from product
    </select>
    <insert id="insert">
        insert into product(name,price,stock,detail,stat,createtime,updatetime)
        values(#{name},#{price},#{stock},#{detail},#{stat},now(),now())
    </insert>-->
</mapper>
